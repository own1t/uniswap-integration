import React from "react";

import { Container, Text } from "./LoadingContainerElements";

const LoadingContainer = () => {
  return (
    <Container>
      <Text>Loading...</Text>
    </Container>
  );
};

export default LoadingContainer;
